<?php

class Project_statesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('project_states')->truncate();

		$project_states = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('project_states')->insert($project_states);
	}

}
