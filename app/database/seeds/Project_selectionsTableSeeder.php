<?php

class Project_selectionsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('project_selections')->truncate();

		$project_selections = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('project_selections')->insert($project_selections);
	}

}
