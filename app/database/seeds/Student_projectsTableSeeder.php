<?php

class Student_projectsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('student_projects')->truncate();

		$student_projects = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('student_projects')->insert($student_projects);
	}

}
