<?php

class Project_application_areasTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('project_application_areas')->truncate();

		$project_application_areas = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('project_application_areas')->insert($project_application_areas);
	}

}
